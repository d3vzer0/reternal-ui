# Git files not required in container
.git
.gitignore
README.md
LICENSE

# No need to copy dockerfiles either
Dockerfile
docker-compose.yml

# If the reternal started and build manually (testing), 
# don't copy the cache to container
node_modules
__pycache__/
.DS_Store


